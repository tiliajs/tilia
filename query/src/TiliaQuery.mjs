// Generated by ReScript, PLEASE EDIT WITH CARE


let Channel = {};

let sortedStringify = (function sortedStringify(value) {
  return JSON.stringify(value, function(_key, value) {
    if (value && typeof value === "object" && !Array.isArray(value)) {
      const sorted = {};
      for (const key of Object.keys(value).sort()) {
        sorted[key] = value[key];
      }
      return sorted;
    }
    return value;
  });
});

function make(_id, _matches, _remote, _local, _expiryOpt, _nowOpt, _keyOpt, _sortOpt) {
  return {
    one: _query => "loading",
    array: _query => ({
      state: "loaded",
      data: []
    }),
    upsert: _value => {},
    remove: _id => {},
    receive: {
      changed: _values => {},
      removed: _ids => {}
    },
    status: {
      pending: 0,
      rejected: []
    },
    retry: _rejection => {},
    discard: _rejection => {},
    tick: () => {},
    dispose: () => {},
    _canopy: () => ({
      live: [],
      idle: []
    })
  };
}

export {
  Channel,
  sortedStringify,
  make,
}
/* No side effect */
