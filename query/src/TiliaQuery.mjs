// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Tilia from "tilia/src/Tilia.mjs";
import * as Belt_Array from "@rescript/runtime/lib/es6/Belt_Array.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";

async function run(data, key, set) {
  let list = await data.fetch(key);
  let ids = list.map(item => {
    let id = data.id(item);
    data.objects.set(id, item);
    return id;
  });
  return set({
    TAG: "Loaded",
    _0: ids
  });
}

function make(id, fetch, param) {
  let data_objects = new Map();
  let data_queries = new Map();
  let data = {
    id: id,
    fetch: fetch,
    objects: data_objects,
    queries: data_queries
  };
  let get = id => {
    let item = data_objects.get(id);
    if (item == null) {
      return "NotFound";
    } else {
      return {
        TAG: "Loaded",
        _0: item
      };
    }
  };
  let find = key => {
    let query = data_queries.get(key);
    let tmp;
    let exit = 0;
    if (query == null) {
      exit = 1;
    } else {
      tmp = query;
    }
    if (exit === 1) {
      let match = Tilia.signal("Loading");
      let query$1 = match[0];
      data_queries.set(key, query$1);
      run(data, key, match[1]);
      tmp = query$1;
    }
    return tmp.value;
  };
  return {
    get: get,
    find: find
  };
}

function toArray(repo) {
  return list => {
    if (typeof list !== "object") {
      if (list === "Loading") {
        return "Loading";
      } else {
        return "NotFound";
      }
    }
    let loaded = Stdlib_Array.reduce(list._0, [], (acc, id) => {
      let item = repo.get(id);
      if (typeof item !== "object") {
        return acc;
      } else {
        return Belt_Array.concatMany([
          acc,
          [item._0]
        ]);
      }
    });
    return {
      TAG: "Loaded",
      _0: loaded
    };
  };
}

export {
  make,
  toArray,
}
/* Tilia Not a pure module */
